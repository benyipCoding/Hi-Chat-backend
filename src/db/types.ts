// export type FriendshipStatus = 'accept' | 'pending' | 'reject';

export enum FriendshipStatus {
  ACCEPT = 'accept',
  PENDING = 'pending',
  REJECT = 'reject',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}
