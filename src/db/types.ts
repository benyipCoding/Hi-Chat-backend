// export type FriendshipStatus = 'accept' | 'pending' | 'reject';

export enum FriendshipStatus {
  ACCEPT = 'accept',
  PENDING = 'pending',
  SENDED = 'sended',
  REJECT = 'reject',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}
